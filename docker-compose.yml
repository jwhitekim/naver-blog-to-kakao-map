services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - backend/.env
    volumes:
      - ./backend/config/settings.yaml:/app/backend/config/settings.yaml:ro
    restart: unless-stopped
